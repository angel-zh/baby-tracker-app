{% block content %}
<nav class="navbar navbar-expand-md navbar-dark fixed-top" style="background-color: #73ceb1;">
    <div class="container-fluid">
        <a href="/" class="navbar-brand fs-3">LilBean</a>
        {% if loggedIn %}
        <a href="/babies" class="nav-link text-light fs-5">Hello {{ username }} </a>
        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if loggedIn %}
                <a href="/babies" class="nav-link text-light fs-5">My Babies</a>
                <a href="/babies/new" class="nav-link text-light fs-5">New Baby</a>
                <a href="/auth/logout" class="nav-link text-light fs-5">Log Out</a>
            {% else %}
            <div class="container-fluid d-flex justify-content-end">
                <a href="/auth/signup" class="btn btn-outline-light mx-2">Sign Up</a>
                <a href="/auth/login" class="btn btn-outline-light">Log In</a>
            {% endif %}
            </div>
        </div>
    </div>
</nav>
{% endblock %}